# Ideas

- ~~only output in verbose mode~~ (done: silent by default, `-v` enables logs)
- ~~better error messages so opencode doesn't get confused~~ (done: X-Ollamux identity header on every response; errors say what happened, why, and what to do — self-identify via /_health too)